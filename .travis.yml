language: python

python:
 - "3.6"
services:
  - postgresql
env:
  global:
    APP_SETTINGS = "test_db"

install:
 - pip install -r requirements.txt
 - pip install pytest-cov

script:
 - py.test --cov=api

after_script:
 - coveralls
 